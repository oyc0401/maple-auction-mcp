# 캡틴(챌린저스) 스탯 재구성 — 진행 상황 (미완)

`docs/stat-residuals.md`의 방법론(소스 특정 → 잔차 0)을 캡틴/챌린저스로 확장하는 작업.
검증 캐릭터: **꽈숩노**(캡틴, 챌린저스, Lv281). 기존에 검증한 카데나·보우마스터는 일반서버라
챌린저스 전용 소스가 엔진에 없었음.

## 2026-07-12 머지 후속 (main) — 7/12 재실측 기준

PR 머지 후 main에서 재구성:
- **jobPassive.ts 삭제 → `JOB_RULES['캡틴']` 파싱 룰로 포팅** (skillPassive.ts). 만렙 상수 대신
  effect 현재 수치 파싱 — 꽈숩노는 스킬레벨+로 컨티뉴얼 에이밍 27%/21%·크루 커맨더십 26%·
  디그니티 공31이라, 파싱 시 **최종뎀 잔차 0**(64.59 정확 일치. 상수로는 -2.59였음).
- 파이렛 블레스: 힘·민첩·지력·운 4스탯 개별 파싱으로 변경(올스탯으로 뭉개지 않음). `mult:3`은
  잔차 실측값이라 유지 중 — 챌린저스 버프 올스탯과 축퇴, 인게임 버프창 실측으로 확정 필요.
- 팬텀 티어 일반화 검증: 중복 티어 텍스트는 정상 세트에도 흔함(보스 장신구 3=5, 칠흑 4=7·5=8,
  광휘 2=5, 에테르넬 6=8) → "중복=팬텀" 규칙 불가. 세트명 지정 캡(SET_MAX_COUNT)이 유일하게 안전.

**7/12 재실측 잔차 (PR 작성 시점과 달라짐 — 캐릭터 상태 변화 추정, 원인 유저 확인 필요):**
```
데미지 · 보스뎀 · 상태이상뎀 · 방무 · 최종뎀 · INT · LUK  → 잔차 0
STR  -115 · DEX -1303        ← PR 때와 동일 계열 (파이렛 해적 보너스 추정)
공격력 -783 · 마력 -121       ← PR 때(-268/-75)보다 커짐
크확 -25 · 크뎀 -8            ← PR 때는 잔차 0이었다고 기록됨
```

## (이하 PR 시점 기록)

## 현재 상태: 13개 항목 중 11개 잔차 0 ✅

```
데미지 · 보스뎀 · 상태이상뎀 · 방무 · 크확 · 크뎀 · 최종뎀 · INT · LUK  → 잔차 0
STR  -115  (특화 65)   ← 미해결
DEX  -1300 (특화 285)  ← 미해결
공격력 -268 · 마력 -75  ← 미해결
```

## 이번에 해소한 챌린저스 전용 이슈

| 소스 | 처리 | 검증 |
|---|---|---|
| **챌린저스 서버 상시버프** | `collectChallenger` (`world_name==='챌린저스'` 게이팅). 올100·공마80·보공70·방무70·크확30·크뎀40 | 크확/보스뎀 잔차 0으로 확인 |
| **버닝 BEYOND / 하이퍼 버닝 MAX** | `collectBurning`. 옵트인이라 스킬 보유로 게이팅. 상호배타. 올30·공마30·보공20·방무20. **효과가 "스킬창에 표시되지 않음" → /skill API에 수치 없어 툴팁 실측 하드코딩** | 보스뎀·방무 잔차 0 |
| **캡틴 직업 패시브** | `JOB_PASSIVES['캡틴']` (jobPassive.ts). 1~4차 무조건 상시분: 크확60·크뎀50·최종뎀62%·공100·주스탯·방무20 | 크확/크뎀/최종뎀 잔차 0(소수점 일치) |
| **도전자 세트 팬텀 8세트** | `SET_MAX_COUNT`로 7세트 캡 (collect.ts). 넥슨 API가 total_set_count 8 + 6세트 복제한 팬텀 8세트(방무10%)를 주는 버그. 인게임 세트창 7/7 실측 | 방무 잔차 0 |
| **파이렛 블레스(본인 해적 링크)** | `LINK['파이렛 블레스']` 룰 수정 + 직업 접미사("(캡틴)") 매칭 (linkSkill.ts). 올스탯30×3중첩=90. 챌린저스는 타직업 링크 불가, 본인 링크만 적용 | INT/LUK 잔차 0 |

## 남은 일 (TODO)

### 1. STR/DEX 특화 잔차 (STR +65, DEX +285) — 파이렛 블레스 해적 추가스탯 추정
- 파이렛 블레스 효과창엔 "올스탯 30"만 뜨지만, 스킬 설명에 **"7레벨: 모험가 해적 직업군은 더 많은 스탯 증가"** — 해적이 받으면 API 미표시 추가 주스탯 보너스가 붙는 것으로 추정(버닝BEYOND가 스킬창 미표시였던 것과 동종).
- STR·DEX 둘 다 **더 필요**(덧셈)라서 단순 힘↔민첩 스왑 아님(스왑은 부위별로 돌려보면 DEX −967로 오히려 손해).
- **해야 할 것:** 인게임 파이렛 블레스 버프 툴팁의 실제 힘/민첩 수치 확인 → `collectLinkSkills`에서 해적 보너스 반영. (유저 확인 대기)

### 2. 공격력 +268 / 마력 +75 잔차
- 파이렛 블레스는 공격력 미부여 → 별개 소스.
- 크리/보스 계열은 전부 잔차 0이라 그 계열 버프/도핑은 없음. 공격력·마력만 남음.
- **해야 할 것:** 스탯창(넥슨 API) 스냅샷에 공격력 도핑(세이람·명예 등) 여부 확인, 또는 미수집 상시 공격력 소스 특정. (유저 확인 대기)

### 3. 검증 직업 회귀 확인
- `linkSkill.ts` 파이렛 블레스 룰의 `mult:3`(최대 3중첩 가정)이 일반서버 캐릭에서 과다계상될 수 있음 — 스택 수가 API에 없어 하드코딩. 계정별 해적 수에 따라 달라짐.
- 5차 `collectFifthBrackets`는 이미 upstream에 존재(로프커넥트/블링크/로디드다이스 등 [패시브 효과] 걷음) — 캡틴 작업 중 재확인함.
- **해야 할 것:** 오유찬(카데나)·티엘(보우마스터) 캐시로 재구성 돌려 잔차 0 유지되는지 회귀 확인.

## 재현 방법
```
# .env 에 NEXON_DEVELOPER_KEY 필요 (레포에 커밋 금지)
npx tsx server/src/damage/cli.ts 꽈숩노
# → .nexon-raw-꽈숩노.json 캐시 사용, 소스별 기여 + 재구성 vs API 잔차 출력
```
